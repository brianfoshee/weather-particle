{
    "event": "weather.test.01",
    "url": "https://api.brianfoshee.com/weather",
    "requestType": "POST",
    "json": {
        "weather": {
          "temperature": "{{temperature}}",
          "humidity": "{{humidity}}",
          "pressure": "{{pressure}}",
          "altitude": "{{altitude}}",
          "created_at": "{{SPARK_PUBLISHED_AT}}",
          "photon_id": "{{SPARK_CORE_ID}}"
        }
    },
    "mydevices": true
}
